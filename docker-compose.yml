version: '3.7'

services:
  reverse-proxy:
    build:
      context: .
    ports:
      - "80:80"
      - "443:443"
      - "8080:8080"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    restart: unless-stopped

networks:
    default:
        external:
            name: traefik